# Test-only values (safe to commit). Copied/loaded by Vitest via test/setup.js.
NODE_ENV=test
JWT_SECRET=test-jwt-secret-for-vitest-only-32chars
ADMIN_EMAIL=admin@test.com
ADMIN_PASSWORD=AdminPass1!
SENDER_EMAIL=noreply@test.com
STRIPE_SECRET_KEY=sk_test_integration_mock
STRIPE_CURRENCY=egp
# Used when integration tests set memory=false (dedicated local DB)
MONGODB_URI=mongodb://127.0.0.1:27017/clinivo_test
